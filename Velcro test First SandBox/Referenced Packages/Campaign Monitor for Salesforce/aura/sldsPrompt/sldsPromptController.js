({
    openPrompt : function(component, event, helper){
        helper.toggleVisibility(component, true);
    },

    closePrompt : function(component, event, helper){
        helper.toggleVisibility(component, false);
    }
})